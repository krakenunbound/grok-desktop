{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window — full app IPC + core plugins",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default",
    "core:webview:default",
    "core:app:default",
    "core:path:default",
    "core:tray:default",
    "core:menu:default",
    "core:resources:default",
    "core:image:default",
    "dialog:default",
    "allow-all-grok-commands"
  ]
}
